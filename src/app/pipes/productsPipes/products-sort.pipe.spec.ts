import { ProductsSortPipe } from './products-sort.pipe';

describe('ProductsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
